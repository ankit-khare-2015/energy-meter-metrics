apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    isDefault: true
    url: postgres:5432
    database: analytics
    user: dbtuser
    secureJsonData:
      password: dbtpass
    jsonData:
      sslmode: disable
